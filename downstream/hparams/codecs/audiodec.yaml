# ###########################################################################################
# Codec: AudioDec
# Authors: Luca Della Libera 2025
# ###########################################################################################

experiment_name: audiodec

# Codec parameters
num_codebooks: 2
vocab_size: 1024
embedding_dim: 64
model_name: symAD_libritts_24000_hop300

# Codec
codec: !new:audiocodecs.AudioDec
    sample_rate: !ref <sample_rate>
    num_codebooks: !ref <num_codebooks>
    model_name: !ref <model_name>
    mode: !ref <mode>
